{
  "name": "library-management",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "install-pyinstaller": "python -m pip install pyinstaller",
    "build-exe": "python -m PyInstaller --onefile --windowed --icon=app_icon.ico main.py"
  }
}